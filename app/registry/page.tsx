import { AppShell } from '@/components/layout/app-shell'
import { RegistryAnalytics } from '@/components/screens/registry-analytics'

export default function RegistryPage() {
  return (
    <AppShell>
      <RegistryAnalytics />
    </AppShell>
  )
}
